import React, { ReactNode } from "react";


const CodeComponent = ({ children }: {children: ReactNode}) => {
  return (
    <div>
      azeazea
      { children }
    </div>
  )
}

export default CodeComponent;